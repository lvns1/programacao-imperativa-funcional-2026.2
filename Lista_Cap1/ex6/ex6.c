#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

int main()
{
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);
    int a=1, b=2, c=3;

printf("0s números são: %d %d %d\n", a, b, c);
system("pause");
}